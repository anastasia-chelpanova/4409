#pragma once

#include <vector>

float GetArithmeticMean(
	const std::vector<float>& sample);

float GetGeometricMean(
	const std::vector<float>& sample);

float GetHarmonicMean(
	const std::vector<float>& sample);

float GetSquareMean(
	const std::vector<float>& sample);
